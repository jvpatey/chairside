export {
  canRenderWorkerMap,
  getMapboxAccessToken,
  isMapboxMapSupported,
  isMapboxNativeModuleAvailable,
  loadMapboxModule,
} from './mapbox';

export function initializeMapbox(): boolean {
  return false;
}
